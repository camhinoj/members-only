<%= form_for :post do |f| %>

  <%= f.hidden_field :user_id, value: @current_user.id %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :post_body %>
  <%= f.text_area :post_body %>

  <%= f.submit "Post" %>
<% end %>